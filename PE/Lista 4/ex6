#include <stdio.h>

int main() {
	
	int i, c = 0;
	float v[5], soma = 0;

	/* criando vetor de tam 5, contando negativos e somando positivos */

	for (i=0; i < 5; i++){
		scanf("%f", &v[i]);
		if (v[i]/1 < 0){
			c = c + 1;
		}
		else soma = soma + v[i];
	}
	printf("\n%d\n%f", c, soma);
}