#include <stdio.h>
#include <string.h>
#include <math.h>
int main() {
	
	int i, j, m[3][3], soma = 0;

	for (i = 0; i<3; i++){
		for (j = 0; j<3; j++){
			scanf("%d", &m[i][j]);
			if (i < j){
				soma = soma + m[i][j];
			}
		}
	}

	printf("Soma: %d", soma);
}
