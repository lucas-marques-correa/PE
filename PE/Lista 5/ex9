#include <stdio.h>
#include <string.h>
#include <math.h>
int main() {
	
	int i, j, m[4][4], soma = 0;

	for (i = 0; i<4; i++){
		for (j = 0; j<4; j++){
			scanf("%d", &m[i][j]);
			if (i != j && i+j != 3){
				soma = soma + m[i][j];
			}
		}
	}

	printf("Soma: %d", soma);
}
